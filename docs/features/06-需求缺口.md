# 06 需求缺口

设计文档承诺、当前**没有代码**或只有占位的部分。按阻塞关系排序——第一条
堵住了后面两条。

## 1. Telegram 接入通用消息回传（M3 缺口）

M2 已完成平台无关协议、`POST /api/native/events`、owner 校验、归一化、编辑/删除、
最终 id alias 和 ACK/重试语义；但 telegram-tt fork 目前仍然**只调用** im-hub
翻译，尚未发出这些事件。后果：

- 路线 B 里收发的消息不进 `messages` 表
- 客户档案(§8)和关键词告警(§9)的数据源为空——**这两个核心卖点在
  套壳路线下无法工作**,直到补上这条回报链路

M3 方向：fork 增加集中式 outbox 模块，把收发、编辑、删除和 id remap 事件交给
`window.imHubNativeBridge`；收到 `event.ack` 后移除，失败用同一 eventId 重试。

消息 id 是接线前必须解决的阻断：数据库去重键为
`(account_id, platform_message_id)`，所以协议要求 `platformMessageId` 在账号范围内
稳定且唯一。Telegram 的 id 只在 chat 内唯一，而且 TDLib 当前值与 telegram-tt 的
MTP 原始值编码不同；M3 必须让两条来源统一生成 `chatId:serverMessageId`（具体编码需
用真实 fixture 验证），同步规范化 reply/delete/remap，并迁移已有 TDLib 数据。
在 telegram-tt 尚未发事件的当前状态不会产生双路重复；进入 shadow 回传后若不先统一
该键，则同一条消息一定可能落成两行，不能把现有 alias 表当作已经解决。

平台编辑版本目前只有 `editedAt`；相同时间粒度内连续编辑无法可靠判先后。M3 应优先
传平台单调 edit/version 序号，或定义同时间戳事件的确定次序，完成前不能宣称快速连续
编辑已经闭环。

### 固定翻译输入坞桥接

M2 的 `NativeComposerBridge` 与外壳状态机已经完成。当前保持禁用是因为 Telegram
fork 尚未发 `bridge.ready/context.changed`，属于 M3 平台接线，不是通用桥接缺失。

### M3 生产安全门槛

- 删除 fork 页面内 12 小时 JWT 的 `window.__IM_HUB__` 历史注入，改由宿主代理翻译
  或签发窄权限、短时能力；typed bridge 未暴露 JWT 不等于旧通道已经消失。
- 服务端签发并由 Electron 主进程验证 account-control grant；不能把 renderer 的
  owner/auditor 判断当安全边界，角色或归属变化时必须即时撤销。
- `bridge.ready` 绑定平台客户端实际登录账号的稳定 external id，阻止 DB 账号 A 的
  partition 实际登录账号 B 后仍被当作 A 存档和发送。
- `composer.send` 使用稳定 attempt id 做结果幂等；超时属于结果未知，不能盲重试。
- 删除账号时退出平台会话并清除 `persist:native-<accountId>`；对 outbox 做同账号
  single-flight/窗口限流和连接池压力测试。

## 2. 客户档案(设计 §8)— 未开工

设计要求:14 类字段(name / age_range / location / timezone / language /
occupation / family / interests / category_preference / budget_level / stage /
next_followup_at / taboo_topics / interaction / notes);**AI 与人工物理分离**
(`ai_summary` vs `manual_fields`,人工改过的永不被 AI 覆盖);触发 =
会话静默 10 分钟入队 + 每日兜底 + 手动重生成;Claude 结构化输出。

现状:`customer_profiles` 表未建;桌面端右栏
(`renderer/components/CustomerPanel.tsx`)是空壳界面。

## 3. 关键词告警(设计 §9)— 未开工

设计要求:字面量规则编译成 Aho-Corasick + 正则逐条;manager/auditor/owner
默认收,agent 申请-审批(`alert_permissions`);通道 = WS + 后台列表 +
邮件/企业微信 webhook;**必须有确认闭环**(open → acked)。

现状:`keyword_rules` / `alerts` / `alert_permissions` 表未建,零代码。

## 4. 审计日志(设计 §3、§14)— 只有信号位

`resolveScope` 给 auditor 算出 `requiresAudit: true`,但无任何消费方。
设计 §14 还要求:员工首次登录的监控知情确认也要写 `audit_logs`——这是
合规硬要求(员工通讯监控需事先书面告知),不是可选项。

## 5. 打包分发 — 开发期形态

- telegram-tt 靠同级仓库 `npm run dev`(:1234),未纳入安装包
- 补丁版客户端的 GPL 源码交付流程未建
- 服务端部署(docker-compose)未在生产验证
- RUNBOOK「上线前必做」:换 JWT_SECRET、清演示账号、seed 不能跑生产库

## 6. 路线 A 的媒体与显示名

TDLib 适配器只认 `messageText`,图片/文件/语音**静默丢弃**;
`senderDisplayName` 恒为 null;Telegram 群的 `conversationDisplayName`
也是 null(库里显示裸 id)。若路线 B 全面接管,这条按放弃处理;若工作台
还要用,这是首要补课。

## 7. 其他平台

- **Signal**：`signal-cli` 适配器、归一化和测试仍在，可完成关联设备与基础消息
  链路；但目标要求的原生多开、翻译、图片/贴纸保留和消息回传尚未完成，计划 M5。
  `docs/attic/signal-desktop-patches/` 只保存早期原型，不能视为可运行代码。
- **WhatsApp(§5.4)**：只有 `PLATFORMS` 枚举预留，计划 M6。
- **Zoom(§5.3)**：只有类型预留，明确延后到 M8，不阻塞首期交付。

## 8. 已确认的小缺口(RUNBOOK「已知限制」同步)

- 翻译全失败时 UI 停在「翻译中…」,不提示放弃
- WebSocket 断线不自动重连
