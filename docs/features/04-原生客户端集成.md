# 04 原生客户端集成(telegram-tt fork)

## 需求出处

- pivot spec `2026-08-25-native-client-pivot.md`:自建界面永远追不上平台能力
  (实测群显示成 `-1004429183741`、图片在归一化层被丢弃),转向套壳打过补丁
  的开源客户端。「补丁客户端本身成为新的适配器」。

## 实现状态

| 功能 | 状态 |
|---|---|
| telegram-tt fork(GPL-3.0,基线 e58cca3)带 im-hub 补丁 | ✅ |
| Electron `<webview>` 宿主 + 多账号 partition 隔离 | ✅ |
| dom-ready 注入 `window.__IM_HUB__ = {serverUrl, token}` | ✅ |
| 群组/图片/语音/贴纸/回复引用 | ✅(原生自带,零成本) |
| 单一“会话”入口 + 平台/账号两级导航 | ❌ M1 |
| 当前原生会话同步到右侧客户档案 | ❌ M2 |
| 打包分发(静态产物随安装包) | ❌ 开发期依赖 `npm run dev`(:1234) |
| 消息回报 im-hub 服务端 | ❌ 最大缺口,见 06 |

## 实现位置

**桌面端宿主** — `packages/desktop/src/renderer/components/NativeClient.tsx`
- `WEB_CLIENT.telegram = 'http://localhost:1234/'`(开发期指向 Vite)
- 每账号 `<webview partition="persist:native-<accountId>">`,**建过就不卸载**,
  只用 display 切换(重载要重连、重拉会话、丢滚动位置)
- `dom-ready` 时 `executeJavaScript` 注入配置并**回读确认**——注入是翻译
  链路第一环,静默失败会让后面所有开关看起来都对、就是没译文
- webview 支持性检测 + 20 秒加载超时提示 + console 转发(webview 是独立
  渲染进程,不转发就完全看不见里面的报错)

**fork 仓库** — `../telegram-tt`(独立 git 仓库,上游自带 CLAUDE.md 要遵守)

新增文件(冲突面为零):
- `src/util/imhub.ts` — 全部 im-hub 调用的收口(配置读取、翻译、检测、
  draft bridge);拿不到配置时**静默降级成不翻译**,不抛异常
- `src/components/middle/composer/ImHubComposer.tsx` + `.module.scss` —
  发送前校对区(见 03)

修改的上游文件(每处都有 `im-hub 补丁` 注释):

| 文件 | 补丁做了什么 |
|---|---|
| `middle/MiddleColumn.tsx` | 原生输入框下方挂 `<ImHubComposer>` |
| `middle/MessageList.tsx` | 自动翻译强制开;目标固定 `'zh'` |
| `middle/message/Message.tsx` | 译文渲染在原文下方不替换;出站消息也翻 |
| `middle/message/_message-content.scss` | `.imhub-translation` 样式(横线分隔) |
| `common/Composer.tsx` | draft bridge 注册:setDraft/getDraft/原生发送 |
| `global/actions/api/messages.ts` | `translateMessages` 改道 im-hub 网关 |
| `global/selectors/chats.ts` | 去掉语言检测的 Premium 门禁 |
| `global/selectors/messages.ts` | `selectCanTranslateMessage` 认 im-hub 开关 |
| `global/initialState.ts` | `canTranslate` 默认 true(原默认 false 且持久化) |
| `left/settings/SettingsLanguage.tsx` | 去掉翻译设置的 Premium 门禁 |

补丁提交序列(11 个,`git log --oneline` 可见完整演进):从初版浮动卡片
到「贴底一条」到「原生输入框下方独立区域」——形态是跟着用户反馈三次重做的。

## 关键设计取舍

- **fork 源码而不是扒 `web.telegram.org` 的 DOM**:官方页面混淆过,选择器
  一改版就失效,而且失效时不报错、只是悄悄不翻译。
- **补丁最小化**:所有 im-hub 调用收在两个新增文件里,不得不改上游时逐处
  注释——为的是跟上游 rebase 时冲突可控。
- **webview 里的内容当不可信**:它拿不到 `window.imHub`、没有 node 能力;
  注入只给 `{serverUrl, token}` 这一个最小面。
- **用 `executeJavaScript` 而非 webview preload 注入**:preload 要单独走
  一遍构建配置;dom-ready 注入远早于用户点开会话触发翻译,够用。

## 已知限制

- 当前 `App.tsx` 仍把自绘“会话工作台”和“原生界面”作为两个入口，尚未落实
  M0 确认的统一会话结构。
- 本文只描述当前 Telegram 原生实现。Signal 与 WhatsApp 分别在 M5、M6 接入，
  它们不能简单复用 Telegram webview 方案，必须沿用同一桥接协议但使用合适宿主。
- **GPL-3.0 义务**:分发补丁版需向使用者提供对应源码;Telegram 品牌/图标
  另有商标约束。上线前要过一次法务。
- 非官方客户端有平台 ToS 风险(pivot spec 已接受的代价)。
- 开发期依赖同级仓库 + Vite dev server,打包分发未完成(见 06)。
